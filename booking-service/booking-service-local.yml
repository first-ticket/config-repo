server:
  port: 8083

spring:
  kafka:
    bootstrap-servers: localhost:29092

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

messaging:
  outbox:
    enabled: true
    scheduler:
      enabled: false
