# 로컬 환경에서 모든 서비스에 적용
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/firstticket
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  
  data:
    redis:
      host: localhost
      port: 6379

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/